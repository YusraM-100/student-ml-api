## Summary
<!-- One or two sentences: what does this PR do and why? -->

## Changes
-
-
-

## Testing Performed
- [ ] Ran `pytest` locally — all tests passing
- [ ] Verified `/health` manually
- [ ] Verified `/predict` manually (valid input + invalid input)
- [ ] Verified `docker build` succeeds locally

## Docker Impact
<!-- Does this PR change the Dockerfile, base image, exposed port,
     dependencies, or image size? If not, write "No Docker impact." -->

## Checklist
- [ ] Application runs locally
- [ ] Tests pass locally
- [ ] Docker image builds successfully
- [ ] No credentials are committed
- [ ] API health endpoint works
- [ ] Code is ready for review
